{
  "name": "encrypted-timeseries-listener",
  "version": "1.0.0",
  "description": "Listener service - decrypts stream, validates, saves to MongoDB",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "mongodb": "^6.3.0",
    "socket.io": "^4.7.2"
  }
}
